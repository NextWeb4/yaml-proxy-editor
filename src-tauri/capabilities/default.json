{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the local YAML proxy editor",
  "windows": ["main"],
  "permissions": [
    "dialog:default"
  ]
}

